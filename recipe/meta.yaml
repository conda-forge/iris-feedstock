{% set python_min = "3.11" %}
{% set version = "3.15.0rc0" %}

package:
  name: iris
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/s/scitools-iris/scitools_iris-{{ version }}.tar.gz
  sha256: 1233889560a22ee920eff9242e935dcb935a7a053ac39b9e752781bedde4897a

build:
  noarch: python
  script: python -m pip install . -vv --no-deps --no-build-isolation
  number: 0

outputs:
  - name: iris
    requirements:
      build:
        - python                                 # [build_platform != target_platform]
        - cross-python_{{ target_platform }}     # [build_platform != target_platform]
      host:
        - python {{ python_min }}
        - pip
        - setuptools >=77.0.3
        - setuptools-scm >=8
      run:
        - python >={{ python_min }}
        - cartopy >=0.21
        - cf-units >=3.1
        - cftime >=1.5
        - dask-core >=2025.1.0,!=2025.10.0
        - libnetcdf !=4.9.1
        - matplotlib-base >=3.5,!=3.9.1
        - netcdf4
        - numpy >=1.24,!=1.24.3
        - python-xxhash
        - pyproj
        - scipy
        - shapely !=1.8.3

    test:
      requires:
        - pip
        - python {{ python_min }}
      imports:
        - iris
      commands:
        - pip check

  - name: scitools-iris
    build:
      noarch: generic
    requirements:
      run:
        - {{ pin_subpackage('iris', max_pin="x.x.x") }}
    test:
      imports:
        - iris
about:
  home: https://scitools-iris.readthedocs.io/en/stable/
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: Analyse and visualise meteorological and oceanographic data sets.

extra:
  recipe-maintainers:
    - ocefpaf
    - bjlittle
    - lbdreyer
    - pp-mo
    - stephenworsley
    - trexfeathers
    - ESadek-MO
    - HGWright
    - ukmo-ccbunney
